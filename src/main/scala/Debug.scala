/* ------------------------------------------------------------
 !Debug時のみ有効になるコード用便利クラス
 ------------------------------------------------------------ */
package tm8st.util

import scala.util._
import tm8st.util._

/* ------------------------------------------------------------
 !
 !@memo 
 ------------------------------------------------------------ */
object Debug
{
  val isEnable = true

  def apply(block: => Unit)
  {
    if(isEnable){ block }
  }
}
